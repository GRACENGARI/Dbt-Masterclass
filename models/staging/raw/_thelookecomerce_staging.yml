version: 2
models:

  - name: stg_raw__users

    description: One row per user. Light renaming/cleanup only, no joins.

    columns:

      - name: user_id

        description: Primary key.

        data_tests:

          - unique

          - not_null

      - name: email

        data_tests:

          - not_null
